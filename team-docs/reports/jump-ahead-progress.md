# Jump-ahead checkpoints — progress

Phase log. Newest last. Report: `team-docs/reports/jump-ahead-checkpoint-2026-09-15.md`.

2026-09-15 — START: brief = an advanced learner must be able to jump a stage by proving the minimum prerequisite instead of grinding the whole chain. Surveyed the existing engine first: band-level placement (`/kids/placement/*`) and the strict cumulative E3f unit chain (`getLearningPath`) already existed; the missing piece was a "prove it and skip" checkpoint. Decisions taken with the requester: ONE checkpoint covers the whole stage up to the band ceiling (not one per unit); questions derived from the blocking units' OWN published games (no new content to author); a skip is recorded as EXEMPT (`kid_progress.mode='checkpoint'`), never as mastery; the learner attempts and a teacher/admin confirms.

2026-09-15 — CHECKPOINT: backend complete. New `services/checkpointAssessment.js` (pure: derive one question per lesson from each template that states a single correct answer, sample with a guaranteed probe of EVERY unit, refuse a chain too long to cover, grade server-side, verdict = threshold AND per-unit demonstration). New `services/selfPacedSchools.js` (explicit allow-list, not a role) and `services/checkpointPolicy.js` (child → class → school → platform default; a stored scope beats the default in BOTH directions). New `controllers/kidsCheckpoints.js` + 6 routes (issue/submit/status/queue/approve/reject) and 4 policy routes, approve/reject/policy-write gated by `requireStaff`/`requireAdmin`. `kidsSeries.js` refactored: path computation extracted to a shared `computeLearningPath` (so the checkpoint reasons about exactly the chain the path renders) and the done rule now accepts an approved exemption, reporting lesson `state: 'tested_out'` and unit `exempt: true`. Schema = `kids_checkpoint_exams` + `kids_checkpoint_policies`, migration-only (`database/kids-checkpoint-exams-migration.js`, dry-run default) and deliberately absent from `KIDS_CONTENT_TABLES`. Test fixture DDL added; new `test/checkpoint-jump-ahead.test.js` (37 assertions). Gate: 66/66 suites · 761/761 tests; `b3-learning-path` / `e3f-practice-test-gate` / `b3b-age-declaration` green after the refactor.

2026-09-15 — CORRECTION from the requester, applied: the exception is a privilege of the SCHOOL, not of the game. The same `is_global` content is played by normal and flagship schools alike, so the assessment (questions, threshold, coverage, exemption semantics) is identical for everyone and only the CONFIRMER changes; nothing about the content — author, school, category — can grant it; an unresolvable school fails closed to human confirmation. Then: staff must be able to STEER it — a whole-school switch plus class and child grants, so auto-jump can go to a selected fast learner while others stay under close review. Pinned in tests by deep-equalling the issued question set across a supervised and a self-paced school, and by asserting a shared flagship-authored `is_global` lesson stays the player's school privilege.

2026-09-15 — CHECKPOINT: frontend complete. `components/CheckpointTestOut.tsx` (learner card beside the locked group; renders the questions, then pending / unlocked / retry-after; never decides for itself — it renders what the API reports), `pages/Teacher/CheckpointReviews.tsx` at `/teacher/checkpoints` (queue + resolve + one-click child grant), `components/CheckpointPolicyPanel.tsx` (school switch, class/child grant, clear, current permissions). Types, endpoints and i18n keys added; Home's `tested_out` state gets its own badge and is never "Up Next". `tsc` clean · 229/229 vitest · `build:staging` rc=0.

2026-09-15 — OPEN: the migration is NOT applied to live (dry-run reports both tables missing in `elite_kids`; `--apply` needs an explicit order, same rule as the ECCE bridge tables). Rollout is safe meanwhile — a missing table degrades to "no exemptions"/"no policy" and the endpoints answer `503 schema_missing`; verified live that the path helper swallows `Table 'elite_kids.kids_checkpoint_exams' doesn't exist`. Also left alone: no audit-event stream for decisions, and `getUnitSuggestedMode` still uses the legacy `KidTestAttempt` model (untouched, not in scope). Nothing pushed.

2026-09-15 — CHECKPOINT: migration APPLIED to live on explicit order (brief: "apply the checkpoint tables migration to the live kids DB and confirm both tables exist"). Standalone run of `node database/kids-checkpoint-exams-migration.js --apply` from `backend/` — not at server boot, per the DDL rule. Before: dry-run reported `missing: kids_checkpoint_exams, kids_checkpoint_policies` in `elite_kids` (shared `elite_db` untouched). After — confirmed by an INDEPENDENT `information_schema` probe, not the script's own log: `kids_checkpoint_exams` present, 20 cols / 5 indexes / 0 rows; `kids_checkpoint_policies` present, 9 cols / 4 indexes / 0 rows; `elite_db` holds neither table. Post-apply dry-run: `present: both · missing: (none)`. Additive only, empty, no other schema touched.

2026-09-15 — CHECKPOINT (live exposure, so this is not over-claimed): the migration makes the SCHEMA ready, but the feature is not reachable in production yet, because `eb9b527` is still LOCAL — `origin/main` = `5fda5ba`, and `eb9b527` is on no remote branch. The running API (pid 3871810, cwd `/var/www/html/elite/elite-kids/backend`, started 05:21:49Z = the `5fda5ba` deploy, i.e. BEFORE these files existed) answers **404** on `/kids/checkpoint/status`, `/kids/checkpoint/queue`, `/kids/checkpoint/policy`. So the earlier "endpoints answer 503 schema_missing" observation described the local tree, NOT the deployed service — corrected here. The 503 would be what a deployed build returns if the schema were missing; today it 404s because the routes aren't in the running process. Next step, not ordered here: push `eb9b527` and let the deploy restart the API; then the tables are already in place and the checkpoint surfaces go live without a further migration.

2026-09-15 — OPEN: nothing is pushed (per brief). Report updated: `team-docs/reports/jump-ahead-checkpoint-2026-09-15.md` (schema section now records the apply + independent verification).
