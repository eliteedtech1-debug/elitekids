2026-08-28 — Audited the existing five-class scaffold, planning guide, game schemas, validator rules and content-generator prompts. The current library has broad 12-week term spines and sample plans; this expansion will add annual subject grids, lesson-plan specifications and game-ready seed contracts.
2026-08-28 — Applied production rule: one minimum playable game per subject per week, three terms of ten teaching weeks, 1,350-game annual floor across five classes and nine canonical subjects. Added annual coverage matrix, granular lesson-plan template, subject game blueprint, and canonical term naming.
2026-08-28 — Standardized all new framework metadata to `First Term`, `Second Term`, `Third Term`; IDs use `first-term`, `second-term`, `third-term`. Updated class scheme spines from 12 to 10 weeks and added per-week game-seed requirement.
2026-08-28 — Clarified that the 5–10 rule applies to playable game items, not the mathematical quantity inside an item. Added age-scaled number representation progression: objects → groups of five → tally bundles → numerals → number names, with follow-up metadata (`followUpType`, `reinforcementOf`, `representationSequence`). Large ranges such as 40–50 use structured bundles and flexible, age-appropriate modules rather than a fixed five-unit split.
2026-08-28 — Corrected the meaning of `game-chain`: it is a single lesson/unit container for two or more complete game-type components needed to deliver one lesson-plan objective. Components run in intentional order (for example matching → puzzle-split → tap-recognition → drag-sort → quiz), each independently follows the 5–10 playable-item rule, and their counts are not incorrectly summed into one ten-item cap. Added the Maya's Market Basket story example and aligned the schema, backend validation, frontend helpers/renderer, and curriculum contract; `puzzle-split` is now an allowed chain component.
