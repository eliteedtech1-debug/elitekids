# Student Tab Restructure — progress report

**Brief:** implement `team-docs/STUDENT-TAB-RESTRUCTURE.md` (5 tabs, zero overlap)
**Scope:** frontend only (`elite-kids/frontend`). No backend, DB or schema changes.

## Checkpoints

- 2026-09-15 | DONE | Audited current 7-tab layout + confirmed the two bugs: `StatsTab` rendered twice and only under the dead key `'progress'` (STATS was unreachable), and the tab chain ended in a fallback `else` that rendered `PlayTab` for any unknown key. `activeTab === 'home'` also rendered RevisionCard + ReviewZone under HOME.
- 2026-09-15 | DONE | Added `frontend/src/pages/Student/utils/helpers.tsx` — single source for `FloatingDeco`, `getAgeColor`, `SUBJECT_FILTERS`, `HOME_SECTION_LABEL` (was copy-pasted in 4 files; `getAgeColor`/`HOME_SECTION_LABEL`/`playedLessons`/`summary` were dead code in StudentHome).
- 2026-09-15 | DONE | Added `tabs/ReviewTab.tsx` (RevisionCard + `#review-zone` anchor) and `tabs/MeTab.tsx` (StatsTab + Trophy board + Teams, section headings).
- 2026-09-15 | DONE | `StudentHome.tsx`: TABS reduced to home/play/path/review/me; every key has exactly one explicit branch (no fallback `else`); removed the duplicate/unreachable StatsTab blocks; feed keys renamed to tab keys (`review`/`me`/`path`); ReviewDueBadge + FAB "Review Zone" now switch to the REVIEW tab then scroll; collab rail button now opens ME; quick-nav "Jump to Games" opens PLAY.
- 2026-09-15 | DONE | Festival merged into PLAY: `StudentFestival` gained `variant="banner"` (renders nothing until a festival is live, tap unfolds the full board via extracted `FestivalBoard`). FESTIVAL + LEADERBOARD + TEAMS/STATS tabs removed.
- 2026-09-15 | DONE | i18n: added `student.tab.learn`, `student.tab.review`, `student.tab.me` to `chunks/en-s.ts`.
- 2026-09-15 | DONE | Verification: `npx tsc --noEmit` clean; `npx vitest run` 20 files / 229 tests pass (includes the i18n key-integrity gate); `npm run build:staging` succeeds (MeTab 82.4 kB / ReviewTab 15.9 kB / PlayTab 14.2 kB lazy chunks, compat-css guard passed). Build artifacts (`dist.staging/`) removed afterwards. No push, no deploy.

## Behaviour notes for the master

- The weekly goal card (`GoalCard`) and per-game progress list now render inside ME — they were previously unreachable (STATS had no live tab key).
- "Explore subjects" on LEARN now goes to PLAY instead of HOME.
- Side effect worth knowing: `StudentFestival` (components/) imports `FloatingDeco` from `pages/Student/utils/helpers`, so that component now depends on the Student page utility folder.

- 2026-09-15 | DONE | Added `tabs/studentTabs.test.ts` — 21 AST-based tests for the render rules (no DOM env exists in this project, `typescript` is already a devDep, same source-scanning style as `lib/i18n/i18n.test.ts`): TABS = exactly home/play/path/review/me in order; one `activeTab === '<key>'` branch per key; no branch for removed keys (stats/festival/leaderboard/teams/progress); no ternary tab chain (no fallback `else`); PlayTab renders only from the play branch; festival is slotted into PLAY (`festivalBanner` on `<PlayTab>`); one component per tab via an ownership table (RevisionCard/ReviewZone → ReviewTab, StatsTab/TeamsTab/Leaderboard → MeTab, LearningPath → PathTab, CheckpointTestOut → PlayTab, GardenScene → HomeTab); StudentHome imports no tab content; shared helpers declared once in `utils/helpers.tsx`.
- 2026-09-15 | DONE | Mutation-checked the new tests: a stale `activeTab === 'stats'` branch + a `RevisionCard` import in StudentHome produced 3 expected failures; file restored byte-identical afterwards. Helper duplication is scoped to the Student tab surface — `components/BossBattleOverlay|GardenScene|ReviewZone|RevisionCard` still keep private `FloatingDeco` copies at a different opacity (deliberately out of scope).
- 2026-09-15 | VERIFY | `npx tsc --noEmit` clean; `npx vitest run` 21 files / 250 tests pass. No push, no deploy.
- 2026-09-15 | DONE | Browser walk of the five tabs (headless Chromium 152 over raw CDP, staging build served behind an nginx-mirroring proxy, student session minted for test child EK-Q4-TEST-001 @ SCH-ELITE, no DB writes). Full write-up: `team-docs/browser-walk/REPORT.md` (+ report.json, probe.json, shots/*.png). Verified: tab bar = Home/Play(362)/Learn/Review/Me, each tab renders only its own panel, 0 console errors / 0 exceptions / 0 non-2xx after fixes.
- 2026-09-15 | FIX | Three bugs from the walk: (1) HOME was a BLANK screen for a new student (dashboard load records a play day → StreakReminder quiet, empty garden → GardenScene null) — HomeTab now renders a first-time welcome card with a "Pick my first game" CTA wired to PLAY; (2) ME showed a "My Team" heading with nothing under it when the child has no class_code/team_id — MeTab hides that section; (3) offline prefetch storm (81×404 + 259×429): `prefetchAll` swept all 362 lessons ×2 requests against the 300 req/min per-IP limit — now skips cached lessons, caps at 24/visit, stops on 429 and backs off for 60s; StudentHome's inline warm capped at 12 sharing the same back-off. Re-walk: 0 non-2xx.
- 2026-09-15 | DONE | WelcomeSpotlight now points at ME (`student.tab.me` chip instead of "Learning path", new `onOpenGoal` prop wired to `handleTabChange('me')` + scroll; CTA is "Set my goal"; the GoalRing polls up to 6s so it can frame the card after the tab switch). Verified in the browser with CDP response interception (a canned closing + progress response makes the test child a returning student — no DB writes): `mentionsLearningPath=false`, `goalCardPresent/meTabActive/scrolledToGoal = true`.
- 2026-09-15 | DONE | Per-load API budget cut for shared-IP schools. Measured with the harness (budget.json): inline cache warm 12 calls → 1 (one lesson's assets, ≤8); offline sweep 362 lessons ×2 → 6 lessons ×2 deferred 20-60s with a walk-forward cursor (also stopped the 1718-row metadata write per visit); live-feed polling 3 endpoints/30s (6 req/min) → 1 every 3 min + path every 9 min (~0.3/min), skipped while hidden or rate-limited; onboarding status cached after completion; progress fetched once instead of twice; team lookup deferred to ME. **~40 calls/load → 15** (idle 185s: 1 call). Re-ran: 5 tabs still 0 errors / 0 exceptions / 0 non-2xx.
- 2026-09-15 | FLAG | Honest limit, needs a server decision: 15 calls × 30 children opening together ≈ 450 req/min against a 300/min **per-IP** limit shared by the school. Client now fits ~20 kids/min; the rest is server work (rate-limit keyed by school/user, a higher authenticated-kid allowance, or one bootstrap endpoint). Cheapest remaining client wins: `/kids/subscription/status` is called twice per load, `/kids/boss/raid` loads with the dashboard.
- 2026-09-15 | TODO | NOT done (requested twice, then superseded by later briefs): fold the remaining `FloatingDeco` copies in BossBattleOverlay / GardenScene / ReviewZone / RevisionCard into the shared helper. Surveyed: BossBattleOverlay + ReviewZone + RevisionCard use opacity-30, GardenScene uses opacity-20 (the shared default), so the fold needs an `opacity` prop on the shared component and call-site updates (14 call sites). Also still flagged, not changed: PLAY mounts 362 cards / LEARN ~1700 controls in one DOM.

STATUS: done — 5 tabs implemented, typecheck + tests (250) + staging build green.
