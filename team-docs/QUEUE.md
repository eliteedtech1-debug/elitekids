# TASK QUEUE (master-maintained; workers self-dispatch in order)

> **Rows Q48–Q65 added 2026-09-15 by Buffy**, covering the 09-10 → 09-15 work that had been
> recorded only in `reports/*-progress.md`. Every row's evidence path and commit hash is given;
> nothing here is inferred from a session transcript. Coverage was checked against the actual
> commit list for the window, not against the reports — which is how Q61–Q65 were found: five
> commits (the pre-restructure student-dashboard chain, the onboarding/lazy-tab work, two login
> fixes, the band walks) had no row at all.
>
> **Note on mtimes:** `find -newermt` is NOT a reliable signal in this checkout. CI step 1 runs
> `git stash create` + `git reset --hard origin/main` in this same directory, which rewrites the
> mtime of every file it restores — a sweep for "files touched since 09-10" returns 216 files
> and mostly reports the deploy, not the work. Use commits.
>
> **⚠️ Read Q48 before trusting the flagship-pilot reports.** `reports/band-count-progress.md`
> §3–§7 describes plan-JSON and seeder changes (6 bands, `primary`, 2 games/week, ladder
> metadata) that are **not in this repository at HEAD**. Prod demonstrably has the resulting
> content; the source that generated it does not exist in tracked files. See Q48 and Q60.
| # | Task | Assigned | Status |
|

---
|

---


---
|

---


---


---
-|

---


---
--|
| Q1 | B2 media repair (briefs/b2-media-repair.md) | phaseB2 | DONE 2026-08-23 |
| Q2 | C test matrix (briefs/c-test-matrix-expansion.md) | phaseC | DONE |
| Q3 | Asset baseline sweep | fb-review | DONE 2026-08-23 |
| Q4 | B3 kids-web hardening | phaseB2 | DONE 2026-08-23 |
| Q5 | CI runner wiring | phaseC | DONE |
| Q6 | ECCE roadmap spike | phaseC | DONE |
| Q7 | D content factory | phaseD | DONE 2026-08-23 |
| Q8 | S8-4: Auth hardening — series-domestications requireStaff | opencode | DONE 2026-08-26 |
| Q9 | S8-3: kids_curriculum_points renumber — PA-U refs to current units | opencode | DONE 2026-08-26 |
| Q10 | S8-1: i18n P3 — locale files + RTL foundation | opencode | DONE 2026-08-26 |
| Q11 | S8-5: Spaced repetition frontend — SpacedReview.tsx on StudentHome | opencode | DONE 2026-08-26 |
| Q12 | S8-6: Adaptive difficulty frontend — GamePlay uses adaptive profiles | opencode | DONE 2026-08-26 |
| Q13 | S8-2: Content expansion — Animals/Numbers U5-U10 ladder | opencode | DONE 2026-08-26 |
| Q14 | S8-FB1: QA checklist — i18n P3 key audit | fb-review | DONE 2026-09-02 (reports/s8-fb1-i18n-audit.md) |
| Q15 | S8-FB2: QA checklist — Animals/Numbers U5-U10 content verification | fb-review | DONE 2026-09-02 (reports/s8-fb2-content-verification.md) |
| Q16 | S8-FB3: Teacher guide update — spaced repetition docs | fb-review | DONE 2026-09-02 (docs/teacher-game-maker-guide.md; reports/s8-fb3-teacher-guide.md) |
| Q17 | S8-FB4: Copy pass — review card, adaptive hints, locale validation | fb-review | DONE 2026-09-02 (reports/s8-fb4-copy-pass.md) |
| Q18 | E4 Phase 1: WebRTC realtime broadcast | opencode | DONE 2026-09-02 — coturn TURN installed+active via coturn-setup.yml workflow; 3478 relaying; LIVE_WEBRTC=1 in .env (reports/e4-coturn-Q18.md) |
| Q19 | BRIDGE L1-BE: backend engine + content (registry cleanup, label-diagram + stage-sequence schemas/validation, scene v2 backend + scene-library, learning-path + age isolation + goals API, U10a-f series split + sample content) — briefs/be-engine-brief.md | opencode phaseG1 | MERGED 2026-09-03 (commit a316f3c, pushed origin/main — auto-deploy running) |
| Q20 | BRIDGE L2-FE: frontend experience (registry parity, AnalogClock + LabelDiagram + StageSequence renderers, scene visual layer + SceneRenderer + checkpoint, LearningPath + GoalCard replacing All-Games tab) — briefs/fe-experience-brief.md | opencode phaseG2 | MERGED 2026-09-03 (commit a316f3c, pushed origin/main — auto-deploy running) |
| Q21 | BRIDGE L3-QA: advisory audits + gate reports per wave (contract parity, i18n, invariants, live-smoke plan) — briefs/qa-bridge-gaps.md | fb-review | DONE 2026-09-03 (reports/qa-gate-wave1.md — gates green, F-01/F-02 findings, guide copy pass; post-merge live-smoke = MASTER G-W2) |
| Q22 | BRIDGE L4-OPS: MVP→prod DB swap (MVP-TO-PROD-DB-SWAP.md) | MASTER + ROOT | VERIFIED DONE 2026-09-03 (worker takeover) — .env already on elite_db/elite_content/elite_kids since ~Sep 1 (.env.mvp-backup-20260901 rollback exists), prod DBs populated (567 parents; kids rows > _test), daily automated backups, :8484 live on prod. Residual: archive _test DBs after ≥1wk stability (reports/q22-q23-ops-verified.md) |
| Q23 | BRIDGE L4-OPS: Node 20→22 + chat dbm() bug + orphan cleanup | MASTER + ROOT | VERIFIED DONE 2026-09-03 (worker takeover) — Node v22.23.2 live via enabled user unit elite-kids-api.service; chat dbm() lazy-arrow fix committed + socket attached live; orphan FE components gone/used; ParentDashboard PIN→password bug FIXED this session (pending deploy) — reports/q22-q23-ops-verified.md |
| Q24 | Q2 speech slice 3: in-engine speech lessons — SpeechGame embedded mode + GamePlay speech-template bridge (teacher-created speech-letter/word/sentence lessons play + grade end-to-end; no more standalone-only) — briefs/q2-slice3-engine-speech-brief.md | Buffy (worker) | MERGED 2026-09-03 (commit 84fc183, pushed origin/main — auto-deploy ran; reports/q2-slice3-progress.md) |
| Q25 | Q2 speech FE leaf (NO cross-lane deps): Pronunciation Coach + ReadingTracker components over LIVE /kids/speech/assess + /kids/speech/progress endpoints (i18n EN+HA, vitest, no backend changes) — briefs/q2-opencode-lowdep.md | opencode → Buffy (takeover, opencode unavailable) | DONE 2026-09-04 (commit c74d9ae, pushed origin/main — auto-deploy ran; tsc clean, vitest 145/145, build OK; +i18n chunk refactor; reports/q2-speech-fe-progress.md) |
| Q26 | Q2 drawing FE components (Q2-G, NO backend/ML deps): DrawingCanvas + TracingGuide + DrawingFeedback (pure canvas; TensorFlow.js recognition plugs in later, OUT of scope) — briefs/q2-opencode-lowdep.md | opencode → Buffy (takeover) | DONE 2026-09-04 (commit c74d9ae — tsc clean, vitest 145/145, build OK; /student/drawing?mode=demo; reports/q2-speech-fe-progress.md) |
| Q27 | Q2 speech templates story/count (completes Q2-B 5/5) + registry/enum/i18n — QUEUE only AFTER Q24 lands (engine bridge is the play path) | opencode → Buffy (takeover) | DONE 2026-09-04 (commit c74d9ae — VALID_TEMPLATES + enum script + GameCreator cards + GamePlay mode map; Q2-B 5/5; backend 499/2 baseline; reports/q2-speech-fe-progress.md) |
| Q28 | Q2-E Portfolio backend v1 (roadmap §2.7): skill map (ADE v2) + evidence (speech logs + game sessions) + recommendations + JSON export — read-only aggregation, pure helpers, q2-portfolio.test.js 7/7 | Buffy (worker) | MERGED 2026-09-03 (commit daff474 — reports/q2-portfolio-progress.md) |
| Q29 | Q2-E Portfolio frontend (SkillMap + EvidenceGallery + export button on ParentDashboard/Teacher view) | opencode → Buffy (takeover) | DONE 2026-09-04 (commit c74d9ae — SkillMap + EvidenceGallery + 7d + JSON export on ParentDashboard; i18n 23 keys EN+HA; reports/q2-speech-fe-progress.md) |
| Q30 | HA translation review (user = reviewer): session-close list of ~74 low-confidence/unreviewed HA keys from Q24–Q29 i18n — reply with `key: corrected Hausa`; reviewer's fixes then applied + pushed | MASTER (ibagwai9@gmail.com) | EMAILED 2026-09-04 (review file → ibagwai9@gmail.com via elite-kids services/mailer.js using backend/.env SMTP creds — SEND_OK; file: reports/session-close-ha-review-2026-09-04.md; reviewer replies at convenience; fixes applied as Q30 follow-up) |
| Q31 | Wire mailer into elite-kids password-reset + welcome flows: services/mailer.js ALREADY LIVE (backend/.env carries all 6 SMTP_* keys, verified non-empty 2026-09-04; HA-review send proved end-to-end). Note: elite-sms emailConfig.js hardcoded fallback creds are STALE (Gmail 534 app-password required) — elite-sms runs on its own backend/.env creds, NOT the fallback. Remaining: wire send() into reset/welcome flows + mailer unit tests | Buffy (worker) | DONE 2026-09-04 (code wired: OTP + reset-confirm + signup-welcome emails in auth.js; test: backend/test/mailer.test.js — fail-open + rate-limit unit tests; syntax-checked auth.js + mailer.test.js. Full jest DB sweep deferred — no local MySQL root in this env; in-flight changes kept per user, merge/push at next deploy) |
| Q32 | NGEd-game 2027 Q3 "The Village" Class-A BACKEND: collab DDL (kids_teams/kids_team_members/kids_team_challenges/kids_peer_teaching/kids_class_quests) + Sequelize models + pure algorithms (teamFormation, classQuestScoring) + kidsCollaboration controller (9 REST endpoints) + WebSocket hub `/kids/teams/ws` + q3-collab.test.js (+ WS contract) — brief: team-docs/q3-village-planning.md §3.1/§4/\u00a711 | freebuff (worker, AUTONOMOUS) | DONE 2026-09-05 (q3-collab.test.js 15/15, q3-integration.test.js 4/4 verified in full backend gate after deploy-gate fix; ef9b7d7) |
| Q33 | NGEd-game 2027 Q3 "The Village" Class-A FRONTEND: TeamChallenge + PeerTeachingBoard + ClassQuest + CollaborationBadge + useCollaborationSocket hook + StudentHome notification rail + vitest — brief: q3-village-planning.md §3.1 (W4-5) | freebuff (worker, AUTONOMOUS) | DONE 2026-09-05 (frontend vitest 229/229, tsc+build clean in prior frontend-only session; merged earlier) |
| Q34 | NGEd-game 2027 Q3 Parent Intelligence BACKEND: kids_insights + kids_action_items DDL/models + rule-based insightGenerator (8 seed rules) + kidParentIntelligence controller (5 endpoints) + q3-parent.test.js — brief: q3-village-planning.md §3.2 | freebuff (worker, AUTONOMOUS) | DONE 2026-09-05 (q3-parent.test.js 23/23 verified in full backend gate; ef9b7d7) |
| Q35 | NGEd-game 2027 Q3 Parent Intelligence FRONTEND: InsightCard + ActionItem + WeeklyDigest + ComparisonChart + ParentNudge + ParentDashboard redesign — brief: q3-village-planning.md §3.2 (W6-8) | freebuff (worker, AUTONOMOUS) | DONE 2026-09-05 (frontend vitest 229/229, tsc+build clean; merged in prior session) |
| Q36 | NGEd-game 2027 Q3 Teacher AI BACKEND: kids_teacher_insights + kids_content_suggestions DDL/models + teacherAssistant service + kidTeacher controller (5 endpoints) + q3-teacher.test.js — brief: q3-village-planning.md §3.3 | freebuff (worker, AUTONOMOUS) | DONE 2026-09-05 (q3-teacher.test.js 9/9 verified in full backend gate; ef9b7d7) |
| Q37 | NGEd-game 2027 Q3 Teacher AI FRONTEND: TeacherInsightsPanel + StudentAlertCard + ContentSuggestion + AutoAssignDialog + TeacherAnalytics embed — brief: q3-village-planning.md §3.3 (W9-10) | freebuff (worker, AUTONOMOUS) | DONE 2026-09-05 (frontend vitest 229/229, tsc+build clean; merged in prior session) |
| Q38 | NGEd-game 2027 Q3 integration + browser smoke + deploy (q3-integration.test.js end-to-end: class quest → parent digest → teacher report) — q3-village-planning.md §DoD | all | DONE 2026-09-05 (q3-integration.test.js 4/4 verified in full backend gate; ef9b7d7) |
| Q39 | NGEd-game 2027 Q4 Content Marketplace BACKEND (dual-gateway Paystack+Monnify): 3 tables (kids_marketplace_listings/_purchases/_reviews) + kidsMarketplace controller (list/get/create/update/delete/initiate/verify/review + 2 webhooks) + monnifyService + routes + marketplace.test.js — roadmap §2.13 / risk map q4-conflict-risk-map.md. Zero Q3 overlap | opencode | DONE 2026-09-05 (marketplace.test.js 6/6 verified in full backend gate; ef9b7d7) |
| Q40 | NGEd-game 2027 Q4 Content Marketplace FRONTEND: Teacher/Marketplace page + ListingCard + ListingDetail + PublisherDashboard + ReviewForm — roadmap §2.13. Zero Q3 overlap | opencode | DONE 2026-09-05 (frontend vitest 229/229, tsc+build clean; merged in prior session) |
| Q41 | NGEd-game 2027 Q4 Analytics Intelligence BACKEND (additive only): predictiveAnalytics.js + KidPrediction (kids_predictions) + 4 new /kids/analytics/predictions|early-warnings|population|content-effectiveness — roadmap §2.15. BACKEND-safe now; FE (TeacherAnalytics) gated on Q3 merge | opencode | DONE 2026-09-05 (predictive-analytics.test.js 5/5 verified in full backend gate; ef9b7d7) |
| Q42 | NGEd-game 2027 Q4 Offline-First 2.0 isolated slice: lib/offline/* (offlineEngine, conflictResolver, serviceWorker, OfflineProgress.tsx) + POST /kids/sync/delta + GET /kids/sync/schema (additive) — roadmap §2.14. Broad "modify all endpoints/game components" part DEFERRED past Q3 merge | opencode | DONE 2026-09-05 (e2-sync-batch.test.js 5/5 verified in full backend gate; ef9b7d7) |
| Q43 | NGEd-game 2027 Q4 analytics FE (PredictionCard/EarlyWarningPanel/PopulationInsights/ContentScoreboard + TeacherAnalytics embed) + offline-broad (since= on all list endpoints + all 11 game templates offline) — GATED on Q3 merge (shared TeacherAnalytics.tsx) | opencode | DONE 2026-09-05 (frontend vitest 229/229, tsc+build clean; merged in prior session) |
| Q44 | NERDC age band rename (17 files) | opencode | DONE 2026-09-08 (reports/nerdc-age-band-rename-2026-09-08.md) |
| Q45 | ECCE bridge Step 5 completion: observation child/bridge context verification (assertObservationContext; non-flagship roster via SMS API only) + integration proof + GameCreator SMS-context enforcement for non-flagship schools | Buffy (worker) | DONE 2026-09-09 (backend observation-context 11/11 + observation-integration 3/3 + bridge/client regression 13/13; frontend tsc clean, vitest 229/229, build OK; full suite 614/643 — 29F in 6 suites ALL pre-existing Q44 rename fallout, ticketed: Q46) |
| Q46 | Q44 test-corpus reconciliation (deploy-gate blocker): update ageBand/teamFormation/b3b assertions to new NERDC bands, align hermetic kids_children.age_level ENUM + seeds with renamed model, re-baseline b2-story against current stage-sequence schema | Buffy (worker, claimed 2026-09-09) | DONE 2026-09-09 09:10Z — FULL SUITE GREEN: 56/56 suites, 676/676 tests, exit=0 (log c-full-suite-20260909T090818Z.log). Fixes: 9 game-engine schema ageLevel enums (new bands + legacy aliases), kids-routes payload 'Nursery'→'Nursery 1', classToAgeLevel rewrite (accent-fold Crèche, sep/case-insensitive Nur2A/Nursery-2 A etc., arm-suffix strip, Basic 1-6→Primary, Year/Class N≥3→Primary, section-disambiguation fallback), verified 287/287 elite_db.classes class_name map via scripts/q46-verify-class-mapping.js |
| Q47 | Tolerant class-identity matching across the ECCE bridge ("Nur 2 A" ≡ "NUR2-A" ≡ "nursery2a"; sections remain identity) — shared classIdentity util (EliteKids + EliteSMS), wired into client validation, bridge resolution, flagship check, and SMS getKidsLessonContext (canonical-code pinning) | Buffy (worker) | DONE 2026-09-09 (EliteKids bridge/observation/client 44/44; SMS sharedKidsLessonContext 9/9; fixed pre-existing SMS test aud-claim bug) |
| Q47b | Class resolution upgraded to two-tier (exact → canonical → AMBIGUOUS_CLASS fail-closed) after live elite_db.classes inspection: class_name column carries LOWER KG/UPPER KG, BASIC 1-6, section arms (Primary 2 A/B), location suffixes (Fagge/Dala), Crech typo — all canonicalized; sections/ordinals remain identity | Buffy (worker) | DONE 2026-09-09 (EliteKids 60/60, SMS 11/11; helper re-synced to both repos; reports/ecce-bridge-sms-integration-progress.md cont.3) |
| Q48 | **Primary annual pilot**: band-count verification, Primary progressive band + Playgroup one-class-fits-all ladder (6 bands, 2 games/subject/week for Primary, 15-item Primary cap, published directly with is_global=1) | Buffy (worker) | ⚠️ DONE IN PROD 2026-09-10, **NOT REPRODUCIBLE FROM THE REPO** — see drift note below (reports/band-count-progress.md) |
| Q49 | **Student tab restructure**: 5 tabs (HOME/PLAY/LEARN/REVIEW/ME), zero overlap, one branch per key, no fallback `else`; + class-safe load budget (~40 → 15 API calls/load) + FloatingDeco fold + 21 AST contract tests | Buffy (worker) | DONE + DEPLOYED 2026-09-15 (`15f0b34` plan, `ad6dbbb`, `7120fbe`; reports/student-tab-restructure-progress.md, browser-walk/REPORT.md) |
| Q50 | **Play-0 production bug**: Nursery children saw "Play 0" — client ranked lessons on the OLD 5-value legacy ladder while `kids_lessons.age_level` stores NERDC labels, so every NERDC row ranked −1 and was dropped. ONE canonical NERDC six-band ladder accepting both vocabularies; `filterInBand` defers to the server list instead of `[]`. + PLAY ordered by curriculum, not `createdAt` | Buffy (worker) | DONE + DEPLOYED 2026-09-15 (`dd78174`, `797777b`; Demo5 Play 0 → 814; 257 tests) |
| Q51 | **Student data-ownership audit** (directive: no cross-app tables, no duplicated primary data, fetch students via elite-sms APIs) | fb-review (worker) | AUDIT DONE 2026-09-15, IMPLEMENTATION NOT STARTED (reports/data-ownership-students-audit.md — `elite_db.kids_children` clean; **42 direct reads of `elite_db.students` across 17 files** must become API calls; proposed 3-endpoint SMS API + 6-phase migration, no `elite_db` ALTERs) |
| Q52 | **Jump-ahead checkpoints** ("test out" of a locked chain): `services/checkpointAssessment.js` + `selfPacedSchools` + `checkpointPolicy`, `controllers/kidsCheckpoints.js` (6 + 4 policy routes), `computeLearningPath` extracted and shared with the path, exemption = `mode='checkpoint'` never mastery, `CheckpointTestOut.tsx` + `/teacher/checkpoints` + policy panel, `kids_checkpoint_exams`/`kids_checkpoint_policies` | Buffy (worker) | DONE + DEPLOYED 2026-09-15 (`eb9b527`, `cdf088f`) — migration APPLIED to live `elite_kids` (independently verified: exams 20 cols/5 idx, policies 9 cols/4 idx, `elite_db` holds neither), **routes now return 401 (mounted) on live`, 66/66 suites · 761/761 tests. Reminder: `/kids/checkpoint/*` was 404 in production until `eb9b527` reached origin — the earlier "503 schema_missing" observation described the local tree, not the deployed service |
| Q53 | **PLAY subject sections**: group the grid by subject in path/unit order (`groupBySeries`), unit-level sections rejected on data (1350 of 1530 units hold a single game), jump-ahead offer moved onto a locked SUBJECT once | Buffy (worker) | DONE + DEPLOYED 2026-09-15 (`8fc1e11`) — live walk 1718 cards / 52 sections / 6 offers, 0 errors; reports/play-unit-grouping-progress.md, browser-walk/REPORT-PLAY-SECTIONS.md |
| Q54 | **ECCE game bridge**: model binding to `KIDS_DB_NAME` + mount `POST /kids/observations` + playable-item contract in `gameConfigRules` (0176bdc); review/approval state machine `publish-gate`/`approve`/`publish`/`recall` + `BridgeAuthoring.tsx` teacher screen + `bridge-routes`/`bridge-review` suites (535a91e) | Buffy (worker) | DONE + DEPLOYED 2026-09-14 (`0176bdc`, `535a91e`; **2 of the 3 failing suites were real production defects**, not test defects — bridge models were writing to the shared EliteSMS DB; 65/65 · 726/726; reports/ecce-bridge-schema-ownership-and-routes-2026-09-14.md) |
| Q55 | **Deploy hardening** after the 403 docroot incident: gate frontend publishes behind a staging build + atomic release swap (`ef46cbb`); retire the manual rsync scripts and refuse in-place frontend builds (`65d86e3`); and correct the deploy docs to describe the deploy that exists, not a `production` remote (`f62193d`) | Buffy (worker) | DONE + DEPLOYED 2026-09-14 (reports/frontend-403-docroot-incident.md — nginx docroot had no `index.html`; `index index.html` + autoindex off yields 403, not 404) |
| Q56 | **Crèche content + placement commit** so the deploy gate can pass: restored the reverted tracked half of the crèche work from stash `fa375e14`, guarded the Jolly Phonics seeder entry point (`require.main === module`) so a test cannot trigger seeding or its `process.exit`, promoted 4 contract suites from untracked to tracked | Buffy (worker) | DONE 2026-09-15 (`5fda5ba`; gate 65/65 · 724/724; reports/creche-content-placement-gate-fix-2026-09-15.md). Root cause was an asymmetry, not a broken test: CI step 1 runs `git stash create` + `git reset --hard origin/main`, which reverts TRACKED modifications and leaves UNTRACKED files |
| Q57 | **Crèche game audit** — manual audit of seeded Crèche games (adult-led, observation-based; must not present an ambiguous child-facing quiz or demand speech where pointing/gaze/gesture/sound is the intended response) | fb-review (worker) | **GATE 1 EXECUTED 2026-09-15** — the bounded read-only production inventory blocked since 09-11 was run (SELECT-only). **Scope: 1085 early-years configs in `elite_kids`, ALL `published`; 1000 fetched, `truncated: true` (hard MAX_ROWS cap, 85 withheld).** Five findings: **(1)** the audit tool **mis-attributes 100% of what it fetched** — every id is `fp-*` (flagship) but `ownerForRow()` knows only global-catalog/animals/jolly-phonics/placement, so 889 are `unknown` and 111 misfiled as jolly-phonics on a `Letters` category substring; since Phase B says unknown ownership is never auto-repaired, the manifest would **refuse to repair the platform's own content** — a TOOL defect blocking Gate 2, needing no content decision; **(2)** the config's denormalized age uses a different vocabulary from its lesson's, and **the Nursery 2 band's configs declare `age_level='KG1'`** — off by one band, the Q67/Q70 class, and why KG1 rows appear in an early-years query (needs a ruling; not assumed); **(3)** the Milestone 2–8 source fixes are NOT in the stored rows — `missing-adult-observation` critical **1000/1000**, `missing-tap-options` and `missing-explicit-answer-key` critical **108** (exactly the tap set), so the array-position-as-answer-key hazard is still live in every published tap game, and no config carries `assessment: 'adult observation'`; **(4)** **1085/1085 `published`** → Phase C's replacement workflow is the whole job, a content-republication programme, not a JSON patch; **(5)** Phase A needs a keyset continuation. **No write, manifest, seeder or migration**; Gates 2/3 un-granted. Recommended next: add the `fp-` rule + `--after`, re-run, then seek Gate 2. reports/q57-gate1-inventory-2026-09-15.md, reports/creche-legacy-inventory-2026-09-15.json |
| Q58 | **A dashboard load counted as a play day** — `streak.ts#recordPlayDay` POSTed `/kids/economy/streak/record` from `StudentHome.tsx:365` on every mount; the server UPDATEd `kids_economy` (streak_current/longest, freeze_count, last_play_date, multiplier) and could INSERT milestones | Buffy (worker) | **FIXED 2026-09-15, NOT DEPLOYED.** The streak rule is now `kidsEconomy#applyPlayDay`, shared by the endpoint and real play events; `recordPlayDay` became a fire-and-forget wrapper called from `kids.js#recordGameComplete` (`POST /kids/progress/game-complete`, admission from the **JWT**, idempotent per calendar day). The dashboard no longer writes — it refreshes display from the balance **read** via `cacheServerStreak()` — and `GamePlay` records the play day where a game **completes** (after the staff-preview guard). Browser before/after, same child: live writes `['POST …/economy/streak/record']`, new build `[]`; harness gained `noWritesOnLoad` (fails on live, passes on the new build). Backend `test/streak-play-day.test.js` (4) + frontend `streak.test.ts` (6, incl. a call-site guard). Mutation-checked both ways. Gate **68/68 · 782/782**, vitest **276/276**, tsc/staging build clean. reports/play-day-not-dashboard-load-2026-09-15.md. Was: found 2026-09-15 by the live PLAY walk. Play is not required, only opening the app, so streaks inflate and every dashboard visit is a write. **The 09-15 session's "no DB writes" claim was wrong**: it checked `/kids/streak/record` (removed) but the live path is `/kids/economy/streak/record`, which persists — so that session's walks on real children 004 @ SCH/28 and 109 @ SCH/11 advanced their streaks. Finding this also surfaced that **`updateEconomyAfterGame` already advanced the streak** on real activity, but its only caller was the ADE per-item path — the game-complete path had never had a streak caller, so the dashboard POST was surplus for a playing child and the *only* mechanism for a non-playing one. Flagged, not changed: `GET /kids/economy/balance` still INSERTs the `kids_economy` row on read (`last_play_date` stays NULL, streak stays 0), and a game completed offline lands its play day on the **sync** date |
| Q59 | ⚠️ **FINDING — the server-side band cap did not hold.** The 09-15 session found `GET /kids/lessons` returned all 1718 rows to admissions 004/109 where it correctly capped Demo5 at 1085, leaving the CLIENT ceiling as the only defence | Buffy (worker) | **ROOT-CAUSED + FIXED 2026-09-15 (Q67), NOT YET DEPLOYED.** The earlier mechanism written into this row — “their `students.class_name` is empty… a data problem before it is a server bug” — is **WRONG and now disproved**: `elite_db.students.class_name` really holds `'Nursery 2'` (004) and `'Kindergarten'` (109), and **0 of 6649** students rows have an empty `class_name`. The real cause is that `models/Student.js` never DECLARED the column (Q67). Any check must use a mid-band child — a Primary child legitimately receives all rows. **Still open after Q67 and now closed by Q70**: 647 of 6649 children (9.7%) resolved no band at all and were served every band — decorative class names needing the class row's `section` (640), age-word room names (7), and a fail-closed policy for anything left |
| Q60 | **Repo/prod drift on the flagship pilot** (found 2026-09-15): HEAD's `flagship-annual-pilot-plan.json` declared **5 bands and no `primary`**, `git log -S'"primary"'` on it was **empty**, and `flagshipAnnualPilotSeed.js` wrote **one** `content_items` entry per unit — yet prod serves **1718** lessons (the 6-band total) with 2 games per Primary unit | Buffy (worker) | **RESOLVED 2026-09-15 — tracked source now reproduces prod exactly.** Root cause: the 2026-09-10 Primary/Playgroup work (which seeded prod) was never committed on ANY branch (`git log -S'fp-pr-' --all` empty) and was destroyed by the deploy's `git reset --hard origin/main`; three fragments survived (`gameConfigRules.PLAYABLE_ITEM_MAX_PRIMARY`, the game-size doc, a bridges comment). Rebuilt the plan (`primarySubjects`, the `primary` band, `ladder` + `publication` blocks) and the seeder (per-band subjects, slot loop, pedagogy ladder, published posture, 15-item labels), bumped the quiz/stage-sequence schemas 10→15, and rewrote the seed test (3→8 tests). **Verified field-by-field against the DB**: `team-docs/tools/diff-pilot-vs-prod.mjs` → 1710 lessons / 1710 configs / 1530 units / 51 series / 1710 points / 1710 library games all `identical`, `IN SYNC`; gate 67/67 · 772/772. Not deployed (source-only). reports/flagship-pilot-reconciliation-2026-09-15.md. **Was:** prod content is not reproducible from tracked source. Also untracked by policy/design: `curriculum/**` (the whole new tree), `backend/scripts/inventory-creche-legacy.js` + its test, `backend/.env.kids.example` |
| Q61 | **Student dashboard evolution** (pre-restructure, superseded by Q49): separate tests cards vs games into HOME vs a games tab (`b142333`); make Home the games grid and group games by LOCK STATE (`fbc021a`); move RevisionCard + ReviewZone inside HOME only (`e375578`) | Buffy (worker) | DONE + DEPLOYED 2026-09-10 / 09-14 / 09-15. **Superseded**: Q49 replaced this layout with the five-tab split, so read these for history, not for current behaviour |
| Q62 | **Student onboarding + lazy tabs**: split the gender/age step out of the onboarding tour, lazy-load the tab panels, live feed badges | Buffy (worker) | DONE + DEPLOYED 2026-09-15 (`a55be81`) |
| Q63 | **Login / auth surface**: hide the Change School button when the school is auto-detected from the subdomain (`cdd8eae`); show the school logo + name and hide the shortname field on a subdomain (`2924984`) | Buffy (worker) | DONE + DEPLOYED 2026-09-10 / 09-15 |
| Q64 | **Live band walks + student-API end-to-end verification**: drove real children's sessions against live (Nursery 2 @ SCH/28, Kindergarten @ SCH/11, plus Demo5) over raw CDP to confirm per-band Play counts and the five tabs | Buffy (worker) | DONE 2026-09-15 (docs `cb00c02`, `17c0755`). **Produced two of the open findings**: Q59 (the server cap did not hold for those children — all 1718 rows, where Demo5 was capped at 1085) and Q58 (the walks were believed read-only; they were not). ⚠️ If re-run, expect `POST /kids/economy/streak/record` — abort it (see `b8fef89`) |
| Q66 | **`/kids/learning-path` 500s for every SMS-imported child** (found live 2026-09-15 while trying to reach a mid-band child, the one band the first PLAY walk could not cover). `getLearningPath` dereferenced `child.class_code` while `child` is `null` for any child with no `kids_children` row, so a child whose band resolves from the TOUR DECLARATION reached the dereference and 500'd. `computeLearningPath` already coped with the null child for band resolution — the response builder was missed | Buffy (worker) | FIXED 2026-09-15, **NOT DEPLOYED** — `child?.class_code`, plus the regression test the suite was missing: the existing `B3B-IMPORTED` fixture (“students row, NO kids_children row”) only called `resolveBandForAdmission()` directly, never the endpoint. Gate 66/66 · 762/762; mutation-checked (reverting `?.` fails the new test). reports/learning-path-500-sms-imported-fix-2026-09-15.md |
| Q67 | **G6 age ceiling never applied to real-school children** (fixed) (found live 2026-09-15 by re-probing the mid-band children from Q64, the band the first PLAY walk could not cover). `models/Student.js` declared 7 attributes and **`class_name` was not one of them**; Sequelize selects only declared attributes, so `ageBand.resolveBandForAdmission` read `student.class_name === undefined` for every child of a real school. The next fallback, `class_code`, is a synthetic `CLS####` code that `classToAgeLevel` deliberately strips, so **no band resolved at all** → `kids.js` skipped the ceiling (`if (childBand)`) and served the full 1718-lesson catalog. `visibleLevels()` itself was always correct | Buffy (worker) | **DONE + DEPLOYED + WALKED 2026-09-15** (`8b35aa5` → `releases/20260915T154251Z-8b35aa5`). Declared `class_name` + `current_class` on the read-only mirror (SELECT widening only). Against real data, 004 `null → Nursery 2` (1718 → **1085**), 109 `null → Kindergarten` (1718 → **1356**). Gate **67/67 · 767/767**; mutation-checked (reverting the model fails 2 of the 5 new tests, incl. the leaking Kindergarten/Primary content). Bonus finding: **prod `students.current_class` holds the class CODE (`'CLS0876'`), not a name**, so the resolver's documented `current_class` fallback is inert in this data. reports/g6-band-ceiling-model-fix-2026-09-15.md. **Live after deploy**: 004 → Nursery 2 / **1085** lessons (was 1718), 109 → Kindergarten / **1356**, Demo5 → Nursery 1 / **814**; all capped, and 004/109 stopped 400-ing on `/kids/learning-path`. Browser walk of the sectioned PLAY on both real-school mid-band children: 004 1085 cards / 37 sections / 9 offers (no Kindergarten, no Primary), 109 1356 / 46 / 9, 0 errors, `readOnly: true` |
| Q65 | **Schema migration run against the shared `elite_db`, 2026-09-10** — `backend/database/migrate.js` (production-safe runner, dry-run by default, backs up before applying) left three pre-migration backups of `elite_db.school_setup` at 06:10:45Z, 06:11:14Z and 07:09:10Z | unassigned | **CLOSED 2026-09-15 — IT APPLIED NOTHING.** Five runs on 09-10; the only write attempt (06:10:45) FAILED — `Table 'elite_db.kids_lessons' doesn't exist` — and 06:11:14 / 06:39:26 (DRY-RUN) / 07:09:10 are explicit no-ops ("Nothing to do — schema already up to date"). The three `school_setup` backups are **byte-identical** (`md5 778fb156…`) because the runner dumps that table unconditionally even with an empty plan: the backup proves it RAN, not that anything changed. The 18,291→22,155 B growth is **EliteKids' own 08-19 run** (pre-ALTER dump, so `kids_stand_alone`/`kids_url` read as new on timing alone) plus **other Elite apps** on the shared DB (`+enable_offline_mode`, the `require_result_approval` comment, `−cbt_center`). The kids-only rule held **vacuously, by failure, not design**. Impact: **none** — no writes, no drift. The runner's own state defect is Q78. reports/q65-migration-closeout-2026-09-15.md. **Was:** UNVERIFIED — recorded because the evidence exists, not because it is understood.** The backup grew 18,291 → 22,155 bytes versus the 08-19 run, so `school_setup` really changed, and it is a SHARED (non-kids) table. There is **no report and no commit** for it. Needs someone to say what it applied and confirm the kids-only rule was respected (`backend/logs/kids-migration-backups/2026-09-10T*`) |
| Q68 | **PLAY — "each unit's overview/summary game and its closure test"** (the brief's second half, carried as an open TODO since Q53): surface them per unit in PLAY | Buffy (worker) | **CLOSED 2026-09-15 as a verdict, with new evidence.** (a) The **summary/overview game cannot exist as written**: a unit's games are its `content_items` and every template live is quiz 513 / matching 342 / drag-sort 342 / stage-sequence 342 / tap-recognition 171 — no recap/summary type in any band, so producing one is an authoring brief, not a UI change. (b) The **closure test exists and is already surfaced**: it is the unit's own game in `test` mode (the E3f gate is `lessonComplete = testPass`), and the five early bands carry **270 units / 270 games** — the game IS the unit — so PLAY's per-card **Test**, the **passed** badge and the header's `{done}/{total} units` are the closure at the only grain the content has. New findings: the closure contract **is** declared (`config_json.gamePlan.test`) and is **read by nothing** (only reader of `gamePlan` is a different concept in `kidsLessonBridges.js`), Crèche declares `test: null` in all 270 games while the gate requires a passing test, and **`kids_progress` / `kids_test_attempts` / `kids_mastery_progress` are all 0 rows** so the closure path has never fired in production. No code change (see Q69). reports/play-unit-closure-test-verdict-2026-09-15.md, tools/probe-closure-test.mjs |
| Q69 | ⚠️ **DECISION — the content's closure contract contradicts the implemented gate** (found 2026-09-15, Q68). `gamePlan.test` is JSON null for **all 270 Crèche** games but `{…, requiredAfterPractice: true}` for the other **1,440** (Playgroup…Primary), and the whole declaration is unread dead metadata | Buffy (worker) | **RULED + IMPLEMENTED 2026-09-15 (Q72) — the contract is read now, and it required NO re-seed.** Rulings: **(i) Crèche keeps no closure test** — the content was right and the hard-coded gate was wrong (tier 0 is exposure, `successThresholdPct: 0`, and all 270 configs declare `gamePlan.test: null`), so a Crèche unit now closes on a **completed play**; **(ii) `requiredAfterPractice` does NOT return to the gate** — the 2026-09-04 decision stands (practice-first false-locked children who had already passed a test), the field stays reported-never-enforced, and dropping it from the plan is a content change needing its own re-seed order. Original finding: **(i) Crèche closure** — the plan says a Crèche unit has no test while the gate requires a passing test to complete a lesson and unlock the next unit; nothing blocks test mode today (only teacher/parent `kids_mode_locks` can), so Crèche still closes, but blocking tier-0 test mode would make **Crèche unclosable**. **(ii) `requiredAfterPractice`** — restore it in the gate, or drop it from the plan; the gate deliberately ignores it per the 2026-09-04 decision ("a passing TEST alone completes a lesson"). Either fix means editing the plan JSON + seeder and deciding whether to re-seed prod, which would **re-open the repo/prod drift closed in Q60** — hence an explicit order, not a silent patch. Behaviour today is unaffected by the declaration |
| Q70 | **Band-ceiling second pass** — `visibleLevels()` still did not narrow `GET /kids/lessons` for **647 of 6649 children (9.7%)**, who resolved NO band and were therefore served **all six bands** (the Q59/Q67 fix closed the mass case only) | Buffy (worker) | **FIXED 2026-09-15, NOT DEPLOYED.** Measured cause, not guessed: (1) the class row's **`section` was never passed** to `classToAgeLevel`, so the documented decorative-name fallback was **dead code on the resolver path** — 640 children in Islamiyya/Tahfiz schools with sahaba-named classes ('Umar bin Khaddab', 'TAMHEED B'), whose `current_class` is a stripped `CLS####` code; (2) **age-word room names** ('Just 2s', '3s room') matched no rule — 7 children; (3) **an unreadable identity skipped the ceiling entirely**, widening to every band, the exact inverse of ageBand's own risk rule "fall back to the NARROWEST known rank — never widen". Fix: `ageBand.lookupClassRow` supplies the section; an age-word rule anchored to the whole name (mapped through the existing `ageToBand`); and `kids.js` **fails closed to the narrowest band** instead of every band. Verified read-only against production (paced sweep): **640 of 640 recover** (Primary 639, Nursery 1 1) and **0 remain unresolved**, so the fail-closed fallback is a safety net rather than the mechanism. Never-empty widening measured unreachable (every ceiling admits ≥272 lessons) but retained and now explicitly tested. Gate **67/67 · 778/778**; three mutations each caught; `test-db.js` gained the `classes` table the app now reads. reports/band-cap-holds-2026-09-15.md |
| Q71 | **PLAY leads with the child's own band** — the server sends the learning path series-**name ASC**, so 'Crèche — …' led for EVERY child and a Primary child's own six subjects plus their six jump-ahead offers sat ~1350 cards down, behind toddler content they had outgrown (PLAY-FINDING, first measured 2026-09-15) | Buffy (worker) | **FIXED 2026-09-15, NOT DEPLOYED.** `groupBySeries` now orders sections child-relatively via `sectionBandRank` + `compareSectionBand`: **own band first**, then the below-band review ladder nearest-first, unrankable last, path order kept as the stable tie-break. Presentation only — the server's path order, the E3f lock chain, the checkpoint and LEARN are untouched. Measured with the browser harness on the same Primary child: live (old build) own-band index **45 of 52**, first section 'Crèche — Communication and Early Literacy'; staging (new build) index **0**, first section 'Primary — Basic Science and Technology', all 6 offers on the first 6 sections. Counts identical both sides (**1718 / 52 / 6**), `readOnly: true`. The harness now **asserts** the order (`matches.firstSectionOwnBand`, `null` when not applicable) — it fails on live and passes on the new build. tsc clean, vitest **270/270**, staging build + compat-css guard OK; mutation-checked (removing the sort fails exactly the 2 order tests). reports/play-unit-grouping-progress.md (Q71 section) |
| Q72 | **Closure contract made live** — `config_json.gamePlan.test` was written by the flagship seeder into all 1,710 configs and read by NOTHING, so the content declared 'Crèche has no child-facing test' while the gate demanded a passing test from every lesson (and a Crèche unit would have become UNCLOSABLE the day tier-0 test mode were blocked) | Buffy (worker) | **DONE 2026-09-15, NOT DEPLOYED, NO RE-SEED NEEDED.** New `services/closureContract.js` is the ONE rule: only an explicit JSON `null` `gamePlan.test` means 'no test' (a completed play closes the lesson); **everything else fails CLOSED** to the 2026-09-04 rule, so Playgroup…Primary and every non-flagship/teacher-made config behave exactly as before. `requiredAfterPractice` is reported, never enforced; `mode: 'checkpoint'` is a jumped-over game, never a play. Wired into `getCurriculum`, `computeLearningPath` (lesson nodes now serve a `closure` block) and **`POST /kids/progress/game-complete`, which reads the lesson's config and returns the verdict** (`closure.lesson_complete` / `lesson_state`). Because it *reads* data prod already carries, it does not re-open the Q60 repo/prod drift; `kids_progress` is still 0 rows live, so nothing changes retroactively. `test/lesson-closure-contract.test.js` 14 tests; two mutations (gate ignores the declaration, endpoint ignores it) each caught; files byte-identical after the sweep. Gate **69/69 · 796/796** (was 68/782); E3f passes untouched. reports/closure-contract-ruling-2026-09-15.md. Flagged: Playgroup is `tier: 1` so it KEEPS a child-facing quiz the contract doc says it should not have (plan+re-seed decision); the UI still offers a Test for a testless lesson (now has `closure.requires_test: false` to hide it); `getUnitLockStatus`/`getUnitSuggestedMode` still require a pass on their legacy path but no component calls them |
| Q73 | **Playgroup's declared test** (found while making the closure contract live, Q72): the plan gives Playgroup `tier: 1`, so the seeder writes it a child-facing quiz test (`gamePlan.test`, 4 choices, `successThresholdPct: 60`) while the authored contract says the opposite in SEVEN places | Buffy (worker) | **RULED 2026-09-15 — ALIGN: Playgroup declares NO child-facing test** (`test: null`, `assessment: "adult observation"`). Evidence: `game-ready-curriculum-contract.md:69/75/125`, `year-plan-and-game-authoring-standard.md:184/267-268/313` ("For Crèche/Playgroup, set `test: null`"), `subject-game-blueprint.md:12`, `game-size-and-module-standard.md:151` + the `unitPassed()` pseudocode (exposure tier requires an adult observation record, not a passing test), `flagship-pilot-approval.md:69`, and the Playgroup scheme's own 'observation-led'. The plan's own intent agrees (`gameAgeLevel: "Creche"`, average-driven ladder). Root cause: the seeder infers ASSESSMENT MODE from `tier`, a learning-difficulty knob. **NOT APPLIED** — the patch (per-band `assessment` in the plan + seeder reads it for `test` / `assessment` / `successThresholdPct`) is code-only in the repo, but prod's 270 Playgroup configs still declare a test, so committing it without a re-seed re-opens the Q60 drift in exactly the fields `diff-pilot-vs-prod.mjs` compares: edit + re-seed is ONE order. Also exposed: choices 4 vs doc 2-3, `successThresholdPct: 60`, and the standard's exposure-tier closure being an ADULT OBSERVATION RECORD that is not wired to the lock chain (`kids_teacher_observations` / `POST /kids/observations` are read by nothing in `kidsSeries`) — Q72's play-closes rule is the substitute. reports/playgroup-tier-ruling-2026-09-15.md |
| Q74 | **Apply the Q73 ruling + re-seed prod**: per-band `assessment` in the flagship plan, the seeder reading it for `gamePlan.test` / `pilot.assessment` / `successThresholdPct` instead of inferring from `tier` | Buffy (worker) | **DONE 2026-09-15 — 270 PROD ROWS RE-SEEDED, CODE NOT DEPLOYED.** Plan: `assessment: "adult observation"` for creche AND playgroup, `"low-stakes practical and game evidence"` for nursery-1..primary; `assessmentModeForBand()` makes the seeder read it, so `gamePlan.test` is null / `pilot.assessment` is 'adult observation' / `successThresholdPct` is 0 for the exposure tier. Validation now enforces BOTH halves (an undeclared band fails; exposure must be testless with no score gate; every other band must still declare a test), so neither can silently spread. Prod write was verified at every step: snapshot before (`Playgroup 270 with test`), `diff-pilot-vs-prod` pre-flight showed **exactly 270 `kids_game_configs.config_json` mismatches and nothing else**, `--confirm` re-seed, then **IN SYNC** and `Playgroup 270/270 testless` after. Counts unchanged (1710/1710/1530/51/1710/1710). Rollback = revert the two edits + re-seed, or the snapshot artifact. Gate **69/69 · 799/799** (+3 tests). reports/exposure-tier-testless-2026-09-15.md, tools/snapshot-playgroup-closure.mjs |
| Q75 | **Hide the Test action for a testless lesson** (the UI half of Q72/Q74): the child must not be offered a quiz the content declares does not exist | Buffy (worker) | **DONE 2026-09-15, NOT DEPLOYED.** `lessonRequiresTest()` in `lib/utils/learningPath.ts` (+`LessonClosure` on `PathLesson`) is **fail-closed** — only an explicit `requires_test: false` hides the Test. `StudentHome` carries it onto every PLAY card, `PlayTab` renders the card **Test** action only when `requiresTest !== false`, and `GamePlay` hides the Test **tab**, coerces a stale `?mode=test` link to `practice`, and suppresses the 'practice → Test →' next CTA; staff/preview still see Test. tsc clean, vitest **280/280**, `build:staging` + compat-css green; mutation-checked (dropping the PlayTab guard fails the call-site test, file byte-identical after). Flagged: nothing server-side blocks `mode: 'test'` itself, and the TEST-SHEET says the live build still offers it because the code is not deployed. reports/exposure-tier-testless-2026-09-15.md |
| Q76 | ⚠️ **DECISION — should the exposure tier's closure be an adult observation record wired to the lock chain?** (`game-size-and-module-standard.md` `unitPassed()`: *"if ageBand is Creche and exposureOnly: require adult observation record"*) | Buffy (worker) | **RULED 2026-09-15: NO — keep Q72's play-closure as the unlock, the observation stays EVIDENCE.** The lock chain is a pacing device, not an assessment record; the docs' real defect (an unverified exposure closure) is fixed by teachers USING the observation pipeline, not by holding the child's next unit hostage to its data entry. Evidence: live `kids_teacher_observations = 0` (as are `kids_progress` / `kids_test_attempts`, with 4 children), so wiring it would strand every unit on day one — the Q69 hazard made immediate; and **no frontend file calls `/kids/observations`** (`grep -rn 'observations\|OBSERVATION' frontend/src` is empty), so the gate would depend on a screen that does not exist. If ever ordered, it needs BOTH preconditions in order: (1) a teacher 'record observation / mark unit passed' UI over the existing staff endpoints, (2) a per-school opt-in via `services/featureFlags.js` + school allow-list (the `isFlagshipPilotEnabled` pattern, **no schema change**), default OFF with play-closure retained as fallback. Revisit trigger: `kids_teacher_observations > 0` with a UI writing them. reports/exposure-tier-observation-closure-ruling-2026-09-15.md |

| Q77 | **DB-isolation audit** (from Q65): the orphan `elite_db_test_test`, and the two kids tables still in the shared `elite_db` | Buffy (worker) | **AUDITED 2026-09-15 — one orphan, one design exception, one LIVE 500.** (a) **`elite_db_test_test` cannot recur**: `ensureTestSuffix` appended `_test` unconditionally until **`0176bdc` (2026-09-14)**, doubling an already-suffixed `elite_db_test`; the helper is idempotent now (`databaseNames.js:31-35`) with a regression test, and **nothing in the repo references the DB**. It holds 39 tables / 34 `kids_*` = an exact copy of the test fixture set in the kids role. There are **three** stale kids test DBs (`elite_kids_test` current; `elite_content_test`, still the `AI_DB_NAME` fallback; `elite_db_test_test`), and `run-tests.sh:13` still names the wrong one. (b) **The shared-DB kids tables are deliberate** — `kidsLeaderboard.js:88,103` creates them on **`db.sequelize`** (the shared connection), documented in its header — so `databaseNames.js`'s 'DB_NAME -> read-only here' is already false and a future kids-only check must not flag them blindly. (c) **But `kids_badges` is two tables under one name** — `elite_db` (leaderboard: `academic_year`/`term`/`class_code`/`position`/`badge ENUM(gold,silver,bronze)`) vs `elite_kids` (festival/arena/parent: `child_admission_no`/`badge_name`/`badge_emoji`/`badge_type`) — different DB, different schema, both 0 rows. (d) **⚠️ LIVE DEFECT: `kidsAnalytics.js:55` reads `kids_weekly_points` through `dbm().content` (the KIDS DB) where it does not exist** — the exact statement gives `elite_kids` → **ER_NO_SUCH_TABLE**, `elite_db` → OK. `GET /kids/analytics/overview` (`routes/kids.js:427`) wraps the handler in one try/catch returning 500 and the throw precedes `res.json`, so **the entire overview fails for every school**. **One-line fix `dbm().content`→`dbm().sequelize` (NOT applied — investigative brief; highest-value next order).** Also: the same handler hard-codes `JOIN elite_db.students`. reports/db-isolation-audit-2026-09-15.md |
| Q78 | **`backend/database/migrate.js` could not plan or apply anything** | Buffy (worker) | **REPAIRED + VERIFIED 2026-09-15, NOT COMMITTED.** Both defects fixed together: (A) `addContentColumns` was referenced 7× and declared nowhere → ReferenceError before the plan printed; (B) `COLUMN_PLAN` mixed owners (kids tables checked against `DATABASE()` = the MAIN DB) → every run planned 4 doomed ALTERs, the 09-10 failure. Now `COLUMN_PLAN` is **shared-only**, `CONTENT_COLUMN_PLAN` is **kids-only** and gained the four mis-targeted columns, with two PURE exported builders (`plannedAlterStatements`, `buildColumnPlan` — the single place both halves are produced), `main()` reading kids columns from the `content` connection and destructuring both arrays, plus exports and a `require.main` guard. **Verified:** 8 pure specs; an **end-to-end dry-run against the hermetic `_test` DBs prints a plan with ZERO ALTER statements** (previously the 4 kids columns were always planned) and exits 0; two mutations each caught; full gate **69/69 · 799/799**. **⚠️ THIS REPAIR BROKE A DEPLOY: `39e379a`'s job Failed at 19:38:46Z, the frontend publish never ran, so `origin/main` is AHEAD of live (`b3cca8f`)** — the deploy's `git reset --hard` reverted this uncommitted fix while the **untracked** test survived and then correctly failed 7/8 against the broken runner. New lessons: **an untracked file participates in the deploy gate**, and a fix can be **split from its test** by that reset. Mitigated: test parked at `team-docs/tools/migration-runner-plan.test.js` (outside Jest's rootDir), spawnSync case removed for pure specs, full gate re-run green. **NEEDS AN ORDER: commit the runner + test TOGETHER (test moved back to `backend/test/`), then re-run the deploy for `39e379a`.** reports/q78-migration-runner-repair-2026-09-15.md |
