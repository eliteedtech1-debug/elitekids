# B3 — Kids-web hardening
Error boundaries on lesson/game routes; loading states for slow media; mode-lock enforcement UI parity with API (mode=game lock returns data:null today — confirm UX handles it); accessibility pass (tap targets, contrast); smoke-test each JP unit page end-to-end.
