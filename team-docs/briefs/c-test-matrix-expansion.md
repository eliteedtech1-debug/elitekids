# C — Test matrix expansion
Add regression tests locking B1 gains: auth+series flow, get-details contract shape, mode-lock endpoint, game config round-count >=5 invariant; wire into CI-ish runner script; target: fail-set shrinks from 40 pre-existing as unrelated debt gets ticketed separately.
