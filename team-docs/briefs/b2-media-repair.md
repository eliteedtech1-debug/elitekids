# B2 — Media repair
Audit broken/missing images across kids-web pages (hero cards, lesson thumbs, companion sprites); fix paths/fallbacks; verify via HTTP 200 sweep of all referenced assets; record before/after counts in reports/b2-progress.md.
